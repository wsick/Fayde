<UserControl 
    x:Class="WickedSick.Thea.Views.VisualTree"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vms="clr-namespace:WickedSick.Thea.ViewModels">
    <Grid>
        <TreeView ItemsSource="{Binding RootLayers}">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type vms:VisualViewModel}" ItemsSource="{Binding VisualChildren}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Type}" />
                        <TextBlock Margin="5,0,0,0" Text="{Binding Name, StringFormat=[{0}], TargetNullValue=''}" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>