XamlObject
	LogicalParent
	Name
	IsAttached
DependencyObject
	ValueProvider
UIElement
	VisualParent
FrameworkElement
	Mentor?
Control
	IsEnabled

ProviderStore Inheritance
DependencyObject - LocalValue, DefaultValue, AutoCreate
	TextElement - Inherited
	UIElement - Inherited
		FrameworkElement - DynamicValue, LocalStyle, ImplicitStyle, DataContext
			Control - IsEnabled

FrameworkElementDynamicProvider --> ComputeActualSize Dependencies
	Visibility
	VisualParent
	IsLayoutContainer
		RenderSize


1. Metrics - Manages bounds
2. Xformer - Manages transforms
3. UpdateMetrics - Holds cache of metrics used during update
4. UpdatePass - Manages logic to Measure+Arrange+ProcessUp+ProcessDown
		- Add render logic
	

Proposed Structure

XamlObject
	TreeNode (all should be created in same closure and have access to each other)
		UpdatePass		->		LayoutUpdater
			UpdateMetrics	->		Metrics (Merge)
			Metrics			->		Metrics (Merge)
			Xformer			->		Xformer
			(Nothing)		->		Invalidator?
DependencyObject
	ProviderStore
	Expressions


AttachVisualChild
	FE
		UIE
			Invalidate me
			Set child's visual parent + surface
			Propagate inherited properties
			Child's OnAddedToTree
				Invalidate pretty much everything on child
		SetSubtreeNode
			AttachTo
				Gracefully merge namescopes
				Set child's ParentNode to me
				Set child IsAttached to my IsAttached
			My Subtree Node	= child
		Set child IsLoaded to my IsLoaded
			if unloaded -> Clear Implicit styles
			else -> Set Implicit Styles
			Propagate IsLoaded
			if loaded -> Emit DataContext Changed
