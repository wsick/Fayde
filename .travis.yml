language: node_js
node_js:
  - "0.10"
before_install:
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install -g fayde-unify
  - grunt symlink:test
script: grunt test --verbose --force --debug
install:
  - npm install
  - bower install